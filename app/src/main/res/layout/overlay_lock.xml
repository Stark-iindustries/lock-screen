<?xml version="1.0" encoding="utf-8"?>
<!-- Root is transparent on purpose: we never draw over the wallpaper,
     only the glow scanner element and a small hint label. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <com.lockglow.app.FingerprintGlowView
            android:id="@+id/glowTouchTarget"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:clickable="true"
            android:focusable="true" />

        <TextView
            android:id="@+id/hintText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:shadowColor="#B0000000"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="8"
            android:text="@string/unlock_subtitle"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

    </LinearLayout>

</FrameLayout>
